# Test configuration with multiple servers and common ports
# This should test resilience when some configs fail

# Server 1 - Valid configuration
server {
    listen 8080;
    server_name valid.localhost;
    root /Users/alvin/Documents/42_berlin/webserv/pr_file1/www/html;
    index index.html;
    
    location / {
        allowed_methods GET;
    }
}

# Server 2 - Invalid root directory (should fail) - uncomment to test invalid path handling
# server {
#     listen 8081;
#     server_name broken.localhost;
#     root /nonexistent/invalid/path;  # This path doesn't exist
#     index index.html;
    
#     location / {
#         allowed_methods GET;
#     }
# }

# Server 3 - Tries to use same port as Server 1 (should fail) - uncomment to test port conflict handling
# server {
#     listen 8080;  # Port conflict with Server 1
#     server_name conflict.localhost;
#     root /Users/alvin/Documents/42_berlin/webserv/pr_file1/www/html;
#     index index.html;
    
#     location / {
#         allowed_methods GET;
#     }
# }

# Server 4 - Invalid syntax (missing semicolon) - uncomment to test syntax error handling
# server {
# 	listen 8082  #Uncomment line to trigger syntax error
#     server_name syntax.localhost  # Missing semicolons
#     root /Users/alvin/Documents/42_berlin/webserv/pr_file1/www/html
    
#     location / {
#         allowed_methods GET;
#     }
# }